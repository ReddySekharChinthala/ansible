- name: variables
  hosts: localhost
  vars_files:
  - var.yaml
  tasks:
  - name: pritnt the info
    ansible.builtin.debug:
      msg: "Hi I'am learning course {{course}} and duration is {{Duration}}."