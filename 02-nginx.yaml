- name: install and run nginx
  hosts: app
  become: yes
  tasks:
  - name: install the nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: run nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enable: yes
