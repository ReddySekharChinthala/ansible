- name: variables
  hosts: localhost
  vars_files:
  - var.yaml
  tasks:
  - name: print the variable information
    ansible.builtin.debug:
      msg: "Hi Myself {{person}}, currenlty learning {{course}} course. Currently {{tool}} tool is im practing."