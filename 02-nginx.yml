
- name: install and run nginx
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: absent

  - name: starting the service
    ansible.builtin.service:
      name: nginx
      state: stop
      enabled: yes